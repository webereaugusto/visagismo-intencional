---
const whatsappLink = "https://wa.me/5519991103018?text=Olá, gostaria de agendar uma consultoria de visagismo";
---

<!-- Fundo claro -->
<section class="py-28 md:py-36 bg-light">
  <div class="max-w-7xl mx-auto px-6">
    
    <!-- Section Header -->
    <div class="text-center max-w-3xl mx-auto mb-16">
      <span class="inline-flex items-center gap-3 text-accent text-xs tracking-[0.3em] uppercase font-montserrat font-medium justify-center">
        <span class="w-8 h-[1px] bg-accent"></span>
        Para Todos
        <span class="w-8 h-[1px] bg-accent"></span>
      </span>
      <h2 class="font-playfair text-4xl md:text-5xl font-medium mt-6 mb-6 text-dark">
        Visagismo <span class="text-accent italic">Masculino</span> e <span class="text-accent italic">Feminino</span>
      </h2>
      <p class="font-montserrat text-gray-dark text-base leading-relaxed">
        Tanto homens quanto mulheres podem se beneficiar dessa poderosa ferramenta para elevar sua autoestima.
      </p>
    </div>
    
    <!-- Two Columns -->
    <div class="grid md:grid-cols-2 gap-8">
      
      <!-- Masculino -->
      <article class="group relative overflow-hidden">
        <div class="aspect-[3/4] overflow-hidden relative" id="masculino-slider">
          <!-- Slider de Antes/Depois -->
          <div class="relative w-full h-full">
            <!-- Imagem Antes -->
            <img 
              src="/images/visagismo-masculino-antes.webp" 
              alt="Visagismo Masculino Antes - Ery Junior Campinas"
              class="masculino-slide absolute inset-0 w-full h-full object-cover transition-opacity duration-1000 opacity-100"
              data-slide="0"
              loading="lazy"
              decoding="async"
            />
            <!-- Imagem Depois -->
            <img 
              src="/images/visagismo-masculino-depois.webp" 
              alt="Visagismo Masculino Depois - Transformação Ery Junior"
              class="masculino-slide absolute inset-0 w-full h-full object-cover transition-opacity duration-1000 opacity-0"
              data-slide="1"
              loading="lazy"
              decoding="async"
            />
          </div>
          
          <!-- Indicador Antes/Depois -->
          <div class="absolute top-4 left-4 z-20">
            <span id="masculino-label" class="bg-gold text-dark px-3 py-1 text-xs font-montserrat font-semibold uppercase tracking-wider">
              Antes
            </span>
          </div>
          
          <!-- Controles do Slider -->
          <div class="absolute bottom-4 left-1/2 -translate-x-1/2 z-20 flex items-center gap-3">
            <button id="masculino-prev" class="w-8 h-8 flex items-center justify-center border border-gold/50 text-gold hover:bg-gold hover:text-dark transition-all" aria-label="Imagem anterior">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
              </svg>
            </button>
            <div class="flex gap-2">
              <span class="masculino-dot w-2 h-2 rounded-full bg-gold cursor-pointer" data-index="0"></span>
              <span class="masculino-dot w-2 h-2 rounded-full bg-gold/40 cursor-pointer" data-index="1"></span>
            </div>
            <button id="masculino-next" class="w-8 h-8 flex items-center justify-center border border-gold/50 text-gold hover:bg-gold hover:text-dark transition-all" aria-label="Próxima imagem">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
              </svg>
            </button>
          </div>
          
          <div class="absolute inset-0 bg-gradient-to-t from-dark via-dark/50 to-transparent pointer-events-none"></div>
        </div>
        
        <div class="absolute bottom-0 left-0 right-0 p-8 lg:p-12">
          <span class="inline-block text-accent text-xs tracking-[0.2em] uppercase font-montserrat mb-4 border border-accent/50 px-4 py-1">
            Para Ele
          </span>
          
          <h3 class="font-playfair text-3xl text-white font-medium mb-4">
            Visagismo Masculino
          </h3>
          
          <p class="font-montserrat text-gray text-sm leading-relaxed mb-6 max-w-md">
            Valorize sua masculinidade. Destacamos os traços marcantes do seu rosto 
            para uma imagem de autoridade e credibilidade.
          </p>
          
          <ul class="space-y-2 mb-6">
            {["Corte ideal para cada tipo de rosto", "Design de barba e bigode", "Harmonização de sobrancelhas", "Imagem executiva"].map(item => (
              <li class="flex items-center gap-2 text-gray font-montserrat text-sm">
                <span class="w-1 h-1 bg-accent rounded-full"></span>
                <span>{item}</span>
              </li>
            ))}
          </ul>
          
          <a 
            href={whatsappLink}
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center gap-2 bg-accent text-white px-6 py-3 text-sm font-medium tracking-wider uppercase hover:bg-accent-light transition-all"
          >
            <span>Agendar</span>
          </a>
        </div>
      </article>
      
      <!-- Feminino -->
      <article class="group relative overflow-hidden">
        <div class="aspect-[3/4] overflow-hidden relative" id="feminino-slider">
          <!-- Slider de Antes/Depois -->
          <div class="relative w-full h-full">
            <!-- Imagem Antes -->
            <img 
              src="/images/visagismo-feminino-antes.webp" 
              alt="Visagismo Feminino Antes - Ery Junior Campinas"
              class="feminino-slide absolute inset-0 w-full h-full object-cover transition-opacity duration-1000 opacity-100"
              data-slide="0"
              loading="lazy"
              decoding="async"
            />
            <!-- Imagem Depois -->
            <img 
              src="/images/visagismo-feminino-depois.webp" 
              alt="Visagismo Feminino Depois - Transformação Ery Junior"
              class="feminino-slide absolute inset-0 w-full h-full object-cover transition-opacity duration-1000 opacity-0"
              data-slide="1"
              loading="lazy"
              decoding="async"
            />
          </div>
          
          <!-- Indicador Antes/Depois -->
          <div class="absolute top-4 left-4 z-20">
            <span id="feminino-label" class="bg-gold text-dark px-3 py-1 text-xs font-montserrat font-semibold uppercase tracking-wider">
              Antes
            </span>
          </div>
          
          <!-- Controles do Slider -->
          <div class="absolute bottom-4 left-1/2 -translate-x-1/2 z-20 flex items-center gap-3">
            <button id="feminino-prev" class="w-8 h-8 flex items-center justify-center border border-gold/50 text-gold hover:bg-gold hover:text-dark transition-all" aria-label="Imagem anterior">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
              </svg>
            </button>
            <div class="flex gap-2">
              <span class="feminino-dot w-2 h-2 rounded-full bg-gold cursor-pointer" data-index="0"></span>
              <span class="feminino-dot w-2 h-2 rounded-full bg-gold/40 cursor-pointer" data-index="1"></span>
            </div>
            <button id="feminino-next" class="w-8 h-8 flex items-center justify-center border border-gold/50 text-gold hover:bg-gold hover:text-dark transition-all" aria-label="Próxima imagem">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
              </svg>
            </button>
          </div>
          
          <div class="absolute inset-0 bg-gradient-to-t from-dark via-dark/50 to-transparent pointer-events-none"></div>
        </div>
        
        <div class="absolute bottom-0 left-0 right-0 p-8 lg:p-12">
          <span class="inline-block text-accent text-xs tracking-[0.2em] uppercase font-montserrat mb-4 border border-accent/50 px-4 py-1">
            Para Ela
          </span>
          
          <h3 class="font-playfair text-3xl text-white font-medium mb-4">
            Visagismo Feminino
          </h3>
          
          <p class="font-montserrat text-gray text-sm leading-relaxed mb-6 max-w-md">
            Revele sua beleza única. Criamos o visual perfeito 
            que expressa sua essência e feminilidade.
          </p>
          
          <ul class="space-y-2 mb-6">
            {["Análise do formato de rosto", "Coloração que valoriza o tom de pele", "Cortes que realçam a cor dos olhos", "Penteados para ocasiões especiais"].map(item => (
              <li class="flex items-center gap-2 text-gray font-montserrat text-sm">
                <span class="w-1 h-1 bg-accent rounded-full"></span>
                <span>{item}</span>
              </li>
            ))}
          </ul>
          
          <a 
            href={whatsappLink}
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center gap-2 bg-accent text-white px-6 py-3 text-sm font-medium tracking-wider uppercase hover:bg-accent-light transition-all"
          >
            <span>Agendar</span>
          </a>
        </div>
      </article>
      
    </div>
    
  </div>
</section>

<script>
  // Slider Masculino - Antes/Depois
  const masculinoSlides = document.querySelectorAll('.masculino-slide');
  const masculinoDots = document.querySelectorAll('.masculino-dot');
  const masculinoLabel = document.getElementById('masculino-label');
  const masculinoPrev = document.getElementById('masculino-prev');
  const masculinoNext = document.getElementById('masculino-next');
  
  let masculinoCurrentSlide = 0;
  const masculinoLabels = ['Antes', 'Depois'];
  
  function showMasculinoSlide(index: number) {
    masculinoSlides.forEach((slide, i) => {
      if (i === index) {
        slide.classList.remove('opacity-0');
        slide.classList.add('opacity-100');
      } else {
        slide.classList.remove('opacity-100');
        slide.classList.add('opacity-0');
      }
    });
    
    masculinoDots.forEach((dot, i) => {
      if (i === index) {
        dot.classList.remove('bg-gold/40');
        dot.classList.add('bg-gold');
      } else {
        dot.classList.remove('bg-gold');
        dot.classList.add('bg-gold/40');
      }
    });
    
    if (masculinoLabel) {
      masculinoLabel.textContent = masculinoLabels[index];
    }
    
    masculinoCurrentSlide = index;
  }
  
  function nextMasculinoSlide() {
    const next = (masculinoCurrentSlide + 1) % 2;
    showMasculinoSlide(next);
  }
  
  function prevMasculinoSlide() {
    const prev = (masculinoCurrentSlide - 1 + 2) % 2;
    showMasculinoSlide(prev);
  }
  
  // Event listeners
  masculinoPrev?.addEventListener('click', prevMasculinoSlide);
  masculinoNext?.addEventListener('click', nextMasculinoSlide);
  
  masculinoDots.forEach((dot, index) => {
    dot.addEventListener('click', () => showMasculinoSlide(index));
  });
  
  // Auto-rotate every 5 seconds
  setInterval(nextMasculinoSlide, 5000);
  
  // Slider Feminino - Antes/Depois
  const femininoSlides = document.querySelectorAll('.feminino-slide');
  const femininoDots = document.querySelectorAll('.feminino-dot');
  const femininoLabel = document.getElementById('feminino-label');
  const femininoPrev = document.getElementById('feminino-prev');
  const femininoNext = document.getElementById('feminino-next');
  
  let femininoCurrentSlide = 0;
  const femininoLabels = ['Antes', 'Depois'];
  
  function showFemininoSlide(index: number) {
    femininoSlides.forEach((slide, i) => {
      if (i === index) {
        slide.classList.remove('opacity-0');
        slide.classList.add('opacity-100');
      } else {
        slide.classList.remove('opacity-100');
        slide.classList.add('opacity-0');
      }
    });
    
    femininoDots.forEach((dot, i) => {
      if (i === index) {
        dot.classList.remove('bg-gold/40');
        dot.classList.add('bg-gold');
      } else {
        dot.classList.remove('bg-gold');
        dot.classList.add('bg-gold/40');
      }
    });
    
    if (femininoLabel) {
      femininoLabel.textContent = femininoLabels[index];
    }
    
    femininoCurrentSlide = index;
  }
  
  function nextFemininoSlide() {
    const next = (femininoCurrentSlide + 1) % 2;
    showFemininoSlide(next);
  }
  
  function prevFemininoSlide() {
    const prev = (femininoCurrentSlide - 1 + 2) % 2;
    showFemininoSlide(prev);
  }
  
  // Event listeners
  femininoPrev?.addEventListener('click', prevFemininoSlide);
  femininoNext?.addEventListener('click', nextFemininoSlide);
  
  femininoDots.forEach((dot, index) => {
    dot.addEventListener('click', () => showFemininoSlide(index));
  });
  
  // Auto-rotate every 5 seconds (offset by 2.5s from masculino for visual interest)
  setTimeout(() => {
    setInterval(nextFemininoSlide, 5000);
  }, 2500);
</script>
